node_modules
dist
.DS_Store
.playwright-mcp
*.log
# Loose screenshots dropped in the repo root (scratch / verification captures).
# Intentional images can still be tracked from a subfolder (e.g. assets/, public/).
/*.png
